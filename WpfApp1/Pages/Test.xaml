<Page x:Class="WpfApp1.Pages.Test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Test" Loaded="Page_Loaded">

    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Button  Content="Выход" Click="Button_Click"/>
                <Label Content="Фильтр"/>
                <TextBox x:Name="filter" Width="300" TextChanged="TextBox_TextChanged" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            </StackPanel>
            <DataGrid x:Name="table" Width="600"/>
            <Button x:Name="edit" Content="Изменить" DockPanel.Dock="Right" Width="100" Height="30" Click="edit_Click"/>
        </DockPanel>
    </Grid>
</Page>
